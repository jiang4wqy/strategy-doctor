# report

`renderScorecard(card, strategy)` returns:
- Five-dimensional stress coverage with survivor outcomes.
- Three-style score breakdown.
- Death breakdown with risk narratives.
- Parameter prescription.
- Held-out risk score and return deltas.

This explicitly preserves non-improving risk information instead of promising guaranteed gains.

Structured output is available from the CLI with `--format json`.