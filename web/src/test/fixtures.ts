import type {
  AnyStrategyDefinition,
  DiagnoseRequest,
  DiagnosisView,
  StrategyDraft,
} from '../api/types.ts';

export const capabilityFixture: readonly AnyStrategyDefinition[] = [];
export const draftFixture = {} as StrategyDraft;
export const requestFixture = {} as DiagnoseRequest;
export const diagnosisFixture = {} as DiagnosisView;
